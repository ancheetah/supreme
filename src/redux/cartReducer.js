
const cartReducer = ( state = {
                                addedItems: [],
                                totalItems: 0
                            }, action) => {
    return state;
}

export default cartReducer;